create archive of the /etc directory
[root@centos ~]# tar cvf etc.tar /etc
create gzip compressed archive of the /etc folder
[root@centos ~]# tar cvfz etc.tar.gz /etc
create bzip compressed archive of the /etc folder
[root@centos ~]# tar cvfj etc.tar.bz /etc
create xz compressed archive of the /etc folder
[root@centos ~]# tar cvfj etc.tar.xz /etc
[root@centos ~]# tar tf etc.tar
extract(x) the archive(f) to a specific folder(C) with files and directories original permissions(p)
[root@centos ~]# tar xvfp etc.tar -C /root/
